{
  "name": "gemini-chatbot",
  "version": "1.0.0",
  "description": "Gemini Chatbot",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.js",
    "build": "mkdir -p public && cp index.html style.css script.js public/",
    "start": "node --env-file-if-exists=.env server.js",
    "lint": "node --check server.js && node --check worker.js"
  },
  "dependencies": {
    "@google/genai": "latest",
    "express": "^4.21.2",
    "grammy": "^1.46.0"
  }
}
